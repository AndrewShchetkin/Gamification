{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome React Debugger",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}",
        }
    ]
}